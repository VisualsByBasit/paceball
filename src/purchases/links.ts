/**
 * Where the paywall's Terms link goes. Null until a terms page is published —
 * the link is left off rather than pointed somewhere that does not exist.
 */
export const TERMS_URL: string | null = null;
